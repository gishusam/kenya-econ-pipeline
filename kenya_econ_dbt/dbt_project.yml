name: kenya_econ_dbt
version: "1.0.0"
config-version: 2

profile: kenya_econ_dbt

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  staging:
    +schema: staging
    +materialized: view
  mart:
    +schema: mart
    +materialized: table
